**Abstract**
- Tập trung giới thiệu mô hình mới: Transformer
- Giới thiệu kết quả điểm BLEU trên 2 mô hình dịch máy áp dụng kiến trúc kể trên: 28.4 cho dịch từ Anh sang Đức trong bài thi WMT 2014; 41.8 cho bài dịch từ Anh sang Pháp sau khi treo 8 CPUs chạy 3.5 ngày. Huấn luyện cho ra kết quả tốt hơn kể cả với bộ dữ liệu lớn và hạn chế.
1. Introduction
-Các cấu trúc thuật toán xử lý cũ: RNN, LSTM, GRNN
-> Hạn chế: Giới hạn cơ bản của thuật toán tính toán tuần tự vẫn tồn tại, sau khi tính toán để cho ra được giá trị h_t, khi thuật toán tính toán tới được một thứ tự nhất định, các dữ liệu cũ của mô hình training bị "quên" đi
-Cơ chế Attention đã được phát triển để khắc phục nhược điểm này: chủ yếu thực hiện trên RNN
-Đề xuất Transformer, mô hình phù thuộc hoàn toàn vào cơ chế attention.

2. Background
-Mô hình sequence modeling và transduction truyền thống dựa vào RNN/CNN phức tạp bao gồm encoder-decoder
-Các mô hình tốt nhất đều sử dụng cơ chế attention kết nối encoder và decoder
-Extended Neural GPU, ByteNet, ConvS2S sử dụng CNN làm khối xây dựng cơ bản, tính toán song song nhưng số bước để học phụ thuộc xa tăng theo khoảng cách (linearly cho ConvS2S, logarithmically cho ByteNet)
-Transformer giảm xuống còn số bước constant, mặc dù giảm độ phân giải hiệu quả do averaging attention-weighted positions (được bù đắp bởi Multi-Head Attention)
-Self-attention (intra-attention): cơ chế attention liên kết các vị trí khác nhau của một sequence đơn để tính toán representation của sequence đó
-Transformer là mô hình transduction đầu tiên chỉ dựa hoàn toàn vào self-attention, không dùng RNN/CNN

3. Model Architecture

|-3.1. Encoder and Decoder Stacks
-Encoder: Stack 6 layers giống hệt nhau
  + Mỗi layer có 2 sub-layers: 
    * Multi-head self-attention mechanism
    * Position-wise fully connected feed-forward network
  + Sử dụng residual connection quanh mỗi sub-layer, sau đó layer normalization
  + Output của mỗi sub-layer: LayerNorm(x + Sublayer(x))
  + Tất cả sub-layers và embedding layers tạo output dimension d_model = 512

-Decoder: Stack 6 layers giống hệt nhau
  + Mỗi layer có 3 sub-layers (thêm 1 sub-layer so với encoder):
    * Masked multi-head self-attention (sub-layer 1)
    * Multi-head attention nhận output từ encoder (sub-layer 2) 
    * Position-wise feed-forward network (sub-layer 3)
  + Cũng sử dụng residual connection và layer normalization
  + Masking trong self-attention đảm bảo predictions cho vị trí i chỉ phụ thuộc vào outputs đã biết tại các vị trí < i (ngăn chặn leftward information flow)

|-3.2. Attention

    |-3.2.1. Scaled Dot-Product Attention
    Embedding là gì?
    -> Là các giá trị vector đa chiều của 1 token đối với các token khác (Nhúng từ "Từ" --> "Vector")
    Dot-Product ở đây là gì?
    -> Dot-Product chỉ các kết quả nhân từ các trọng số sau khi embedding với vector ngữ cảnh h của mỗi tokens
    Query là gì?
    -> Vector 
    -Input gồm: queries, keys có dimension d_k, và values có dimension d_v
    -Công thức: Attention(Q,K,V) = softmax(QK^T/√d_k)V
    -Giải thích:
      + Tính dot products của query với tất cả keys
      + Chia mỗi kết quả cho √d_k (scaling factor)
      + Áp dụng softmax để có weights cho values
    -Lý do scaling: Khi d_k lớn, dot products có magnitude lớn, đẩy softmax vào vùng gradients cực nhỏ
    -So sánh với Additive attention: Dot-product nhanh hơn và space-efficient hơn (có thể dùng matrix multiplication), nhưng cần scaling khi d_k lớn

    |-3.2.2. Multi-Head Attention
    -Thay vì single attention function với d_model dimensions
    -Thực hiện h lần attention song song với các learned linear projections khác nhau:
      + Queries, keys, values được project xuống d_k, d_k, d_v dimensions (h lần)
      + Mỗi projected version chạy attention song song → d_v-dimensional output
      + Concatenate và project lại
    -Công thức: 
      + MultiHead(Q,K,V) = Concat(head_1,...,head_h)W^O
      + head_i = Attention(QW_i^Q, KW_i^K, VW_i^V)
    -Trong paper: h=8 parallel attention layers (heads), d_k = d_v = d_model/h = 64
    -Ý nghĩa: Cho phép model attend to information từ các representation subspaces khác nhau tại các positions khác nhau
    -Chi phí tính toán tương tự single-head attention với full dimensionality do dimension bị giảm

    |-3.2.3. Application of Attention in our Model
    -Transformer sử dụng multi-head attention theo 3 cách khác nhau:
    1- Encoder-decoder attention layers:
       + Queries từ decoder layer trước
       + Keys và values từ output của encoder
       + Cho phép mọi position trong decoder attend to tất cả positions trong input sequence
       + Mô phỏng encoder-decoder attention mechanisms điển hình trong seq2seq models
    2- Encoder self-attention layers:
       + Keys, values, queries đều từ output của encoder layer trước
       + Mỗi position trong encoder có thể attend to tất cả positions trong encoder layer trước
    3- Decoder self-attention layers:
       + Tương tự encoder nhưng có masking
       + Mỗi position trong decoder chỉ attend to tất cả positions ≤ position đó
       + Ngăn information flow từ tương lai, preserve auto-regressive property
    
|-3.3. Position-wise Feed-Forward Networks
Multilayer-Persceptron là gì?

Feed-Forward Networks là gì?

Postion-wise Feed-Forward Networks là gì?

-Mỗi layer trong encoder và decoder chứa fully connected feed-forward network
-Áp dụng riêng rẽ và giống hệt nhau cho từng position
-Gồm 2 linear transformations với ReLU activation ở giữa:
  + FFN(x) = max(0, xW_1 + b_1)W_2 + b_2
-Linear transformations giống nhau across positions khác nhau, nhưng sử dụng parameters khác nhau giữa các layers
-Có thể coi như hai convolutions với kernel size 1
-Input và output dimensionality d_model = 512
-Inner-layer dimensionality d_ff = 2048

|-3.4. Embeddings and Softmax  
-Sử dụng learned embeddings để convert input tokens và output tokens thành vectors có dimension d_model = 512
-Sử dụng learned linear transformation và softmax để convert decoder output thành predicted next-token probabilities
-Chia sẻ cùng weight matrix giữa 2 embedding layers và pre-softmax linear transformation (tương tự như trong các papers khác)
-Trong embedding layers, nhân weights đó với √d_model

|-3.5. Positional Encoding
Positional Encoding là gì?
-Do model không có recurrence và convolution, cần inject information về relative/absolute position của tokens trong sequence
-"Positional encodings" được thêm vào input embeddings ở đáy encoder và decoder stacks
-Positional encodings có cùng dimension d_model = 512 với embeddings → có thể summed
-Nhiều lựa chọn: learned và fixed positional encodings
-Paper sử dụng sine và cosine functions với frequencies khác nhau:
  + PE(pos, 2i) = sin(pos/10000^(2i/d_model))
  + PE(pos, 2i+1) = cos(pos/10000^(2i/d_model))
  + pos là position, i là dimension
-Lý do chọn sinusoidal version:
  + Có thể cho phép model extrapolate đến sequence lengths dài hơn những gì gặp trong training
  + Với mọi fixed offset k, PE(pos+k) có thể được biểu diễn như linear function của PE(pos)
-Thử nghiệm với learned positional embeddings cho kết quả gần như giống hệt

4. Why Self-Attention 
-So sánh self-attention layers với recurrent và convolutional layers theo 3 tiêu chí:
1- Complexity per layer (độ phức tạp tính toán mỗi layer)
2- Amount of computation có thể parallelized (lượng tính toán song song hóa được) - measured by minimum number of sequential operations required
3- Path length giữa long-range dependencies trong network

-Phân tích chi tiết:
  + Self-Attention: 
    * Complexity: O(n²·d) - n là sequence length, d là representation dimension
    * Sequential Operations: O(1)
    * Maximum Path Length: O(1)
  + Recurrent: 
    * Complexity: O(n·d²)
    * Sequential Operations: O(n)
    * Maximum Path Length: O(n)
  + Convolutional: 
    * Complexity: O(k·n·d²) - k là kernel width
    * Sequential Operations: O(1)
    * Maximum Path Length: O(log_k(n)) với dilated convolutions
  + Self-Attention (restricted):
    * Maximum Path Length: O(n/r) khi chỉ xem xét neighborhood size r

-Kết luận:
  + Khi n < d (thường xảy ra với sentence representations - d=512, 1024), self-attention nhanh hơn recurrent layers
  + Self-attention có constant sequential operations và path length → học long-range dependencies hiệu quả hơn
  + Side-benefit: Self-attention có thể yield more interpretable models (có thể visualize attention distributions)

5. Training

|-5.1. Training data and Batching
-Training Data:
  + WMT 2014 English-German dataset: ~4.5 triệu sentence pairs
    * Sentences được encode bằng byte-pair encoding
    * Shared source-target vocabulary ~37000 tokens
  + WMT 2014 English-French dataset: ~36 triệu sentences
    * Tokens được split thành 32000 word-piece vocabulary
-Batching:
  + Sentence pairs được batch theo approximate sequence length
  + Mỗi training batch chứa ~25000 source tokens và 25000 target tokens

|-5.2. Hardware Schedule
-Hardware: 8 NVIDIA P100 GPUs
-Training time:
  + Base models: ~12 giờ (mỗi training step ~0.4 giây)
  + Big models: 3.5 ngày (mỗi training step ~1.0 giây)
  + Tổng cộng 100,000 steps (~300,000 steps cho big model)

|-5.3. Optimizer
-Sử dụng Adam optimizer với β₁=0.9, β₂=0.98, ε=10⁻⁹
-Vary learning rate trong quá trình training theo công thức:
  lrate = d_model^(-0.5) · min(step_num^(-0.5), step_num · warmup_steps^(-1.5))
-Learning rate tăng linearly trong warmup_steps đầu tiên, sau đó giảm tỷ lệ nghịch với square root của step number
-Sử dụng warmup_steps = 4000

|-5.4. Regularization
-Áp dụng 3 loại regularization:
1- Residual Dropout:
   + Áp dụng dropout cho output của mỗi sub-layer, trước khi add với sub-layer input và normalize
   + Áp dụng dropout cho sums của embeddings và positional encodings trong encoder và decoder stacks
   + Base model: P_drop = 0.1
   
2- Label Smoothing:
   + Sử dụng label smoothing value ε_ls = 0.1 trong training
   + Làm hurt perplexity (model trở nên uncertain hơn)
   + Nhưng cải thiện accuracy và BLEU score
   + Giúp model học generalize tốt hơn

6. Results
|-6.1. Machine Translation
-WMT 2014 English-to-German translation:
  + Big transformer model: BLEU = 28.4
  + Cải thiện hơn 2.0 BLEU so với best previously reported models (bao gồm ensembles)
  + Training cost: 3.5 ngày trên 8 P100 GPUs (fraction của training costs của best models từ literature)
  
-WMT 2014 English-to-French translation:
  + Big model: BLEU = 41.0
  + Outperforms tất cả previously published single models
  + Training cost < 1/4 training cost của previous state-of-the-art model
  
-Base model:
  + Training: 10 minutes/epoch (~12 giờ total)
  + Kết quả vẫn surpasses nhiều published models và ensembles
  
-Ensemble averaging:
  + Sử dụng 8 checkpoints cuối cùng (saved 10 minutes intervals)
  + English-to-German: BLEU = 28.4 → 30.2 (với beam search và length penalty α=0.6)
  + English-to-French: BLEU = 41.0 → 41.8

|-6.2. Model Variations
-Thực hiện ablation study để đánh giá tầm quan trọng của các components khác nhau
-Experiments với variations của Transformer base model:

1- Varying number of attention heads:
   + Single-head attention: giảm 0.9 BLEU (worse quality)
   + Too many heads (h=32): cũng giảm quality
   + h=8 là optimal

2- Reducing attention key size (d_k):
   + Làm hurt model quality
   + Determining compatibility khó hơn khi dimension nhỏ hơn

3- Model size (d_model, d_ff):
   + Bigger models tốt hơn
   + Dropout rất hữu ích trong việc avoid overfitting

4- Replacing sinusoidal positional encoding với learned positional embeddings:
   + Kết quả gần như giống hệt base model
   
-Kết luận: Multi-head attention, appropriate key dimensions, và model size đều quan trọng

|-6.3. English Constituency Parsing 
-Để đánh giá khả năng generalization của Transformer sang tasks khác ngoài machine translation
-Thực hiện experiments trên English constituency parsing
-Task này đặc thù:
  + Output bị constraints mạnh
  + Dài hơn đáng kể so với input
  + RNN seq2seq models chưa có thể achieve state-of-the-art results
  
-Setup:
  + Train trên Wall Street Journal (WSJ) portion của Penn Treebank
  + ~40K training sentences
  + Semi-supervised setting: sử dụng thêm BerkleyParser corpus với ~17M sentences
  
-Kết quả:
  + Mặc dù thiếu task-specific tuning
  + Transformer (4-layer, d_model=1024) đạt kết quả excellent
  + Outperforms tất cả previously reported models NGOẠI TRỪ Recurrent Neural Network Grammar
  + WSJ only setting: F1 = 91.3
  + Semi-supervised setting: F1 = 92.7 (Recurrent Neural Network Grammar đạt 93.3)
  
-Ý nghĩa: Chứng minh Transformer generalizes well sang các tasks khác

7. Conclusion
-Transformer: first sequence transduction model dựa hoàn toàn trên attention, thay thế recurrent layers bằng multi-headed self-attention
-Ưu điểm:
  + Train nhanh hơn đáng kể so với architectures based on recurrent/convolutional layers
  + Đạt state-of-the-art quality trên translation tasks
  + English-to-German: BLEU 28.4 (improve 2.0 BLEU)
  + English-to-French: BLEU 41.8 (new state-of-the-art)
  
-Excited về tương lai của attention-based models:
  + Plan áp dụng cho problems khác ngoài text
  + Plan investigate local, restricted attention mechanisms để efficiently handle large inputs/outputs như images, audio, video
  + Making generation less sequential là một research goal khác
  
-Code availability:
  + Code để train và evaluate models có tại: https://github.com/tensorflow/tensor2tensor

-Tầm quan trọng lịch sử:
  + Paper này đã mở ra kỷ nguyên mới cho NLP và AI
  + Kiến trúc Transformer trở thành nền tảng cho BERT, GPT, T5 và vô số models khác
  + Chứng minh rằng attention mechanism là đủ mạnh, không nhất thiết cần recurrence
  + Đặt nền móng cho Large Language Models (LLMs) hiện đại

References

=====================================================================================
                    TÓM TẮT VÀ PHÂN TÍCH BÀI BÁO "ATTENTION IS ALL YOU NEED"
=====================================================================================

**PHẦN 1: TỔNG QUAN (ABSTRACT)**

Bài báo giới thiệu mô hình mới mang tên: TRANSFORMER

Kết quả đạt được (đánh giá bằng điểm BLEU - chỉ số đo lường chất lượng dịch máy):
• Dịch Anh → Đức: 28.4 điểm (WMT 2014)
• Dịch Anh → Pháp: 41.8 điểm (huấn luyện 3.5 ngày trên 8 GPUs)
• Kết quả vượt trội cả trên bộ dữ liệu lớn và nhỏ

=====================================================================================

**PHẦN 2: GIỚI THIỆU (INTRODUCTION)**

2.1. VẤN ĐỀ CỦA CÁC MÔ HÌNH CŨ

Các mô hình trước đây (RNN, LSTM, GRU) có nhược điểm lớn:
┌─────────────────────────────────────────────────────────────┐
│ TÍNH TOÁN TUẦN TỰ (Sequential Processing)                   │
│ ───────────────────────────────────────────────────────────│
│ Token 1 → Token 2 → Token 3 → ... → Token n                │
│                                                              │
│ ❌ Phải xử lý lần lượt từng token                            │
│ ❌ Không thể song song hóa (parallelize)                    │
│ ❌ Với câu dài, thông tin đầu câu bị "quên" mất            │
│    (vanishing gradient problem)                             │
└─────────────────────────────────────────────────────────────┘

VÍ DỤ: Khi xử lý câu "Con mèo màu đen đang ngủ trên ghế sofa"
- Khi model xử lý đến từ "ghế", nó có thể đã quên thông tin về "con mèo"
- Phải chờ xử lý xong "con" mới xử lý "mèo", rồi mới đến "màu"...

2.2. GIẢI PHÁP CỦA TRANSFORMER

Cơ chế Attention đã được dùng trước đó để cải thiện RNN.
→ Transformer đi xa hơn: BỎ HOÀN TOÀN RNN/CNN, CHỈ DÙNG ATTENTION!

=====================================================================================

**PHẦN 3: BỐI CẢNH LỊCH SỬ (BACKGROUND)**

3.1. Các mô hình truyền thống: RNN/CNN với encoder-decoder
3.2. Các mô hình tốt nhất đều dùng Attention để kết nối encoder-decoder
3.3. Một số mô hình dùng CNN (Extended Neural GPU, ByteNet, ConvS2S):
     ✓ Tính toán song song được
     ✗ Số bước để học mối quan hệ xa tăng theo khoảng cách:
       • ConvS2S: tỷ lệ tuyến tính (linear) với khoảng cách
       • ByteNet: tỷ lệ logarit (logarithmic)
       
3.4. **Transformer khác biệt như thế nào?**
     ✓ Số bước học mối quan hệ: CONSTANT (không phụ thuộc khoảng cách!)
     ✓ Dùng Multi-Head Attention để bù đắp việc averaging
     
3.5. **Self-Attention là gì?**
     → Cơ chế attention giúp các vị trí trong CÙNG một câu "nhìn thấy nhau"
     → Tính toán mối liên hệ giữa các từ với nhau trong cùng 1 sequence

3.6. **Đột phá của Transformer:**
     Mô hình đầu tiên CHỈ DÙNG self-attention, KHÔNG DÙNG RNN/CNN!

=====================================================================================

**PHẦN 4: KIẾN TRÚC MÔ HÌNH (MODEL ARCHITECTURE)**

┌──────────────────────────────────────────────────────────────────────┐
│                    KIẾN TRÚC TỔNG QUAN TRANSFORMER                    │
│                                                                       │
│  INPUT → ENCODER (6 layers) → DECODER (6 layers) → OUTPUT           │
└──────────────────────────────────────────────────────────────────────┘

4.1. ENCODER VÀ DECODER STACKS

════════════════════════════════════════════════════════════════════════
A. ENCODER (Bộ mã hóa)
════════════════════════════════════════════════════════════════════════

Cấu trúc: Chồng 6 layers giống hệt nhau

Mỗi layer có 2 sub-layers:
┌────────────────────────────────────────────────┐
│  Sub-layer 1: Multi-head Self-Attention        │
│  (Giúp các từ "nhìn thấy" nhau trong câu)     │
├────────────────────────────────────────────────┤
│  Sub-layer 2: Feed-Forward Network             │
│  (Xử lý từng vị trí độc lập)                  │
└────────────────────────────────────────────────┘

🔧 KỸ THUẬT QUAN TRỌNG:
• Residual Connection: Output = LayerNorm(x + Sublayer(x))
  → Giúp training dễ hơn, tránh vanishing gradient
• Layer Normalization: Chuẩn hóa output
• Dimension của output: d_model = 512 (ở tất cả các layers)

════════════════════════════════════════════════════════════════════════
B. DECODER (Bộ giải mã)
════════════════════════════════════════════════════════════════════════

Cấu trúc: Chồng 6 layers giống hệt nhau

Mỗi layer có 3 sub-layers (nhiều hơn Encoder 1 sub-layer):
┌────────────────────────────────────────────────────────┐
│  Sub-layer 1: MASKED Multi-head Self-Attention         │
│  (Chỉ nhìn các từ ở TRƯỚC, không nhìn tương lai)      │
├────────────────────────────────────────────────────────┤
│  Sub-layer 2: Multi-head Attention (Encoder-Decoder)   │
│  (Nhận thông tin từ Encoder)                          │
├────────────────────────────────────────────────────────┤
│  Sub-layer 3: Feed-Forward Network                     │
│  (Xử lý từng vị trí độc lập)                          │
└────────────────────────────────────────────────────────┘

⚠️ TẠI SAO CẦN MASKING?
VÍ DỤ: Khi dịch "I love you" → "Tôi yêu bạn"
• Khi tạo từ "yêu", model CHỈ được nhìn "Tôi" (từ đã tạo)
• KHÔNG được nhìn "bạn" (từ chưa tạo - thuộc tương lai)
• Đây là tính chất auto-regressive (tự hồi quy)

=====================================================================================

4.2. CƠ CHẾ ATTENTION (PHẦN QUAN TRỌNG NHẤT!)

════════════════════════════════════════════════════════════════════════
4.2.1. SCALED DOT-PRODUCT ATTENTION
════════════════════════════════════════════════════════════════════════

📚 TRẢ LỜI CÂU HỎI CỦA BẠN:

❓ EMBEDDING LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Embedding = Chuyển đổi từ văn bản sang vector số

VÍ DỤ đơn giản:
• Từ "mèo" → [0.2, 0.8, 0.1, 0.5, ...] (512 số)
• Từ "chó" → [0.3, 0.7, 0.2, 0.4, ...] (512 số)

Tại sao cần Embedding?
→ Máy tính không hiểu chữ, chỉ hiểu số!
→ Embedding biến từ thành vector để máy tính xử lý được
→ Các từ có nghĩa gần nhau sẽ có vector gần nhau

VÍ DỤ thực tế:
"vua" và "hoàng đế" → vectors gần nhau
"vua" và "xe đạp" → vectors xa nhau


❓ DOT-PRODUCT Ở ĐÂY LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Dot-Product (Tích vô hướng) = Phép nhân giữa 2 vectors

Công thức cơ bản:
A = [a₁, a₂, a₃]
B = [b₁, b₂, b₃]
A · B = a₁×b₁ + a₂×b₂ + a₃×b₃

Trong Attention:
→ Tính độ tương đồng giữa Query và Key
→ Kết quả càng lớn = 2 vector càng giống nhau = càng liên quan

VÍ DỤ:
Vector "mèo" · Vector "động vật" = số lớn (liên quan mạnh)
Vector "mèo" · Vector "ô tô" = số nhỏ (ít liên quan)


❓ QUERY, KEY, VALUE LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Hãy tưởng tượng như TÌM KIẾM TRÊN YOUTUBE:

• QUERY (Truy vấn):
  → Điều bạn đang TÌM KIẾM
  → VD: "Cách nấu phở bò"

• KEY (Chìa khóa):
  → TIÊU ĐỀ của mỗi video
  → VD: "Hướng dẫn nấu phở bò chuẩn vị", "Cách làm bánh mì"...

• VALUE (Giá trị):
  → NỘI DUNG thực sự của video
  → Thông tin bạn muốn lấy

CÁCH HOẠT ĐỘNG:
1. So sánh QUERY với tất cả KEYs (bằng dot-product)
2. Key nào giống Query nhất → có điểm cao nhất
3. Lấy VALUE của những Key có điểm cao
4. Kết hợp các VALUE theo tỷ lệ điểm số

Trong Transformer:
• Query = "Từ hiện tại đang xem xét"
• Keys = "Tất cả các từ khác trong câu"
• Values = "Thông tin của các từ đó"


════════════════════════════════════════════════════════════════════════
CÔNG THỨC SCALED DOT-PRODUCT ATTENTION:
════════════════════════════════════════════════════════════════════════

                    Attention(Q, K, V) = softmax(QKᵀ / √dₖ) V

GIẢI THÍCH TỪNG BƯỚC:

Bước 1: QKᵀ
→ Tính dot-product giữa Query và tất cả Keys
→ Kết quả: Ma trận điểm số (scores matrix)

Bước 2: QKᵀ / √dₖ
→ CHIA cho √dₖ (dₖ = 64, nên √dₖ = 8)
→ Tại sao? Để điểm số không quá lớn (tránh softmax bị "bão hòa")

Bước 3: softmax(...)
→ Chuyển điểm số thành xác suất (tổng = 1)
→ Attention weights (trọng số attention)

Bước 4: ... × V
→ Lấy trung bình có trọng số của các Values
→ Kết quả cuối cùng!


VÍ DỤ CỤ THỂ:
Câu: "Con mèo đang ngủ"
Tính attention cho từ "mèo":

Query = vector của "mèo"
Keys = vectors của ["Con", "mèo", "đang", "ngủ"]
Values = vectors của ["Con", "mèo", "đang", "ngủ"]

Điểm số sau dot-product (giả định):
"mèo" với "Con" = 0.1
"mèo" với "mèo" = 0.9  (cao nhất - tự nhìn mình)
"mèo" với "đang" = 0.3
"mèo" với "ngủ" = 0.6  (khá cao - "mèo" liên quan đến "ngủ")

Sau softmax → Attention weights:
[0.05, 0.45, 0.15, 0.35]

Output = 0.05×V_Con + 0.45×V_mèo + 0.15×V_đang + 0.35×V_ngủ


📊 SO SÁNH VỚI ADDITIVE ATTENTION:
┌──────────────────────┬────────────────┬─────────────────┐
│                      │ Dot-Product    │ Additive        │
├──────────────────────┼────────────────┼─────────────────┤
│ Tốc độ               │ ⚡ Rất nhanh   │ 🐌 Chậm hơn     │
│ Hiệu quả bộ nhớ      │ ✓ Tốt         │ ✗ Kém hơn       │
│ Khi dₖ lớn           │ Cần scaling    │ Ổn định hơn     │
└──────────────────────┴────────────────┴─────────────────┘

→ Paper chọn Dot-Product vì nhanh hơn (dùng matrix multiplication)

════════════════════════════════════════════════════════════════════════
4.2.2. MULTI-HEAD ATTENTION (ĐA ĐẦU ATTENTION)
════════════════════════════════════════════════════════════════════════

💡 Ý TƯỞNG CHÍNH:
Thay vì 1 lần attention với 512 dimensions
→ Làm 8 lần attention song song, mỗi lần với 64 dimensions!

┌─────────────────────────────────────────────────────────┐
│         1 Attention (512 dims)                          │
└─────────────────────────────────────────────────────────┘
                         VS
┌─────────┬─────────┬─────────┬─────────┬─────────┬──────┐
│ Head 1  │ Head 2  │ Head 3  │ Head 4  │  ...    │Head 8│
│ (64dim) │ (64dim) │ (64dim) │ (64dim) │         │(64d) │
└─────────┴─────────┴─────────┴─────────┴─────────┴──────┘

TẠI SAO LÀM NHƯ VẬY?
→ Mỗi head học một kiểu mối quan hệ khác nhau!

VÍ DỤ trong câu "The animal didn't cross the street because it was too tired":

Head 1: "it" → "animal" (chủ ngữ)
Head 2: "it" → "tired" (trạng thái)
Head 3: "cross" → "street" (động từ-đối tượng)
Head 4: "too" → "tired" (mức độ)
... và còn nhiều mối quan hệ khác!


CÔNG THỨC:
──────────────────────────────────────────────────────────
MultiHead(Q, K, V) = Concat(head₁, head₂, ..., head₈) Wᴼ

Trong đó:
    headᵢ = Attention(Q·Wᵢᵠ, K·Wᵢᴷ, V·Wᵢⱽ)

Q·Wᵢᵠ: Project Query xuống 64 dimensions cho head thứ i
K·Wᵢᴷ: Project Key xuống 64 dimensions cho head thứ i
V·Wᵢⱽ: Project Value xuống 64 dimensions cho head thứ i


THÔNG SỐ TRONG PAPER:
• Số heads: h = 8
• Dimension mỗi head: dₖ = dᵥ = 64
• Tổng dimension: 8 × 64 = 512 = d_model

⚡ CHI PHÍ TÍNH TOÁN:
Mặc dù có 8 heads, nhưng dimension mỗi head giảm xuống 1/8
→ Chi phí tính toán tương đương 1 head với full dimension!

════════════════════════════════════════════════════════════════════════
4.2.3. ỨNG DỤNG CỦA ATTENTION TRONG TRANSFORMER
════════════════════════════════════════════════════════════════════════

Transformer sử dụng Multi-Head Attention ở 3 vị trí:

┌─────────────────────────────────────────────────────────────┐
│ 1️⃣ ENCODER-DECODER ATTENTION                                │
├─────────────────────────────────────────────────────────────┤
│ • Queries: từ Decoder layer trước                           │
│ • Keys & Values: từ output của Encoder                      │
│                                                             │
│ MỤC ĐÍCH: Decoder "nhìn vào" toàn bộ input sequence        │
│                                                             │
│ VD: Khi dịch "I love you" → "Tôi yêu bạn"                 │
│     Khi tạo từ "yêu", decoder nhìn vào ["I","love","you"] │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 2️⃣ ENCODER SELF-ATTENTION                                   │
├─────────────────────────────────────────────────────────────┤
│ • Q, K, V: đều từ output của encoder layer trước           │
│                                                             │
│ MỤC ĐÍCH: Mỗi từ "nhìn thấy" tất cả các từ khác trong input│
│                                                             │
│ VD: Trong câu "The bank by the river"                      │
│     Từ "bank" nhìn thấy "river" → hiểu là "bờ sông"       │
│     (không phải "ngân hàng")                               │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 3️⃣ DECODER SELF-ATTENTION (MASKED)                          │
├─────────────────────────────────────────────────────────────┤
│ • Q, K, V: từ decoder layer trước                          │
│ • CÓ MASKING: chỉ nhìn các vị trí ≤ vị trí hiện tại       │
│                                                             │
│ MỤC ĐÍCH: Giữ tính auto-regressive                         │
│                                                             │
│ VD: Khi tạo từ "yêu" (vị trí 2)                           │
│     Chỉ được nhìn: "Tôi" (vị trí 1) và "yêu" (vị trí 2)  │
│     KHÔNG được nhìn: "bạn" (vị trí 3 - tương lai)         │
└─────────────────────────────────────────────────────────────┘

=====================================================================================

4.3. POSITION-WISE FEED-FORWARD NETWORKS

📚 TRẢ LỜI CÂU HỎI CỦA BẠN:

❓ MULTI-LAYER PERCEPTRON (MLP) LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MLP = Mạng neural đơn giản gồm nhiều layers kết nối đầy đủ

Cấu trúc cơ bản:
Input Layer → Hidden Layer(s) → Output Layer

VÍ DỤ MLP đơn giản:
┌──────┐     ┌──────┐     ┌──────┐
│  x₁  │────→│  h₁  │────→│  y₁  │
│  x₂  │────→│  h₂  │────→│  y₂  │
│  x₃  │────→│  h₃  │────→│      │
└──────┘     └──────┘     └──────┘
Input(3)    Hidden(3)    Output(2)

Mỗi mũi tên = 1 weight (trọng số)
Mỗi node = 1 activation function


❓ FEED-FORWARD NETWORK LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Feed-Forward = Mạng neural mà thông tin chỉ đi một chiều

Đặc điểm:
• Thông tin: Input → Hidden → Output (một chiều)
• KHÔNG có vòng lặp (không có feedback)
• Đơn giản nhất trong các loại neural networks

┌─────────────────────────────────────┐
│  Input → Layer 1 → Layer 2 → Output │  ✓ Feed-Forward
└─────────────────────────────────────┘

┌─────────────────────────────┐
│  Input ⇄ Hidden ⇄ Output   │  ✗ Recurrent (có vòng lặp)
└─────────────────────────────┘


❓ POSITION-WISE FEED-FORWARD NETWORK LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Position-Wise = Áp dụng CÙNG một network cho TỪNG vị trí độc lập

Hãy tưởng tượng:
Bạn có câu: ["Tôi", "yêu", "mèo"]

Position-Wise FFN = Áp dụng CÙNG một hàm f() cho từng từ:
• output₁ = f("Tôi")
• output₂ = f("yêu")
• output₃ = f("mèo")

Không có tương tác giữa các vị trí!
(Sự tương tác được xử lý bởi Attention layer)


════════════════════════════════════════════════════════════════════════
CẤU TRÚC CHI TIẾT TRONG TRANSFORMER:
════════════════════════════════════════════════════════════════════════

Mỗi layer encoder/decoder có 1 Position-Wise FFN:

CÔNG THỨC:
──────────────────────────────────────────────
FFN(x) = max(0, x·W₁ + b₁)·W₂ + b₂

Hoặc viết rõ hơn:
FFN(x) = ReLU(x·W₁ + b₁)·W₂ + b₂


GIẢI THÍCH TỪNG BƯỚC:
┌────────────────────────────────────────────────┐
│ Bước 1: x·W₁ + b₁                              │
│ → Linear transformation đầu tiên              │
│ → Dimension: 512 → 2048 (mở rộng 4 lần!)     │
├────────────────────────────────────────────────┤
│ Bước 2: ReLU(...)                              │
│ → Activation function                          │
│ → ReLU(x) = max(0, x)                         │
│ → Loại bỏ giá trị âm                          │
├────────────────────────────────────────────────┤
│ Bước 3: ...·W₂ + b₂                           │
│ → Linear transformation thứ hai               │
│ → Dimension: 2048 → 512 (thu nhỏ lại)        │
└────────────────────────────────────────────────┘

THÔNG SỐ:
• Input/Output dimension: d_model = 512
• Hidden dimension: d_ff = 2048
• Kích thước tăng 4 lần rồi giảm về ban đầu

💡 TẠI SAO LÀM NHƯ VẬY?
→ Tăng expressiveness (khả năng biểu diễn)
→ Cho phép model học những pattern phức tạp hơn

🔧 KỸ THUẬT TRIỂN KHAI:
Có thể implement bằng 2 convolutions với kernel size = 1
→ Hiệu quả hơn về mặt tính toán

=====================================================================================

4.4. EMBEDDINGS VÀ SOFTMAX

════════════════════════════════════════════════════════════════════════
EMBEDDING LAYERS:
════════════════════════════════════════════════════════════════════════

Chuyển đổi tokens → vectors 512 chiều

┌──────────────────────────────────────────────┐
│ Input Embedding:                             │
│ "I" → [0.1, 0.5, ..., 0.3] (512 numbers)    │
│ "love" → [0.2, 0.8, ..., 0.1] (512 numbers) │
│ "you" → [0.4, 0.3, ..., 0.7] (512 numbers)  │
└──────────────────────────────────────────────┘

┌──────────────────────────────────────────────┐
│ Output Embedding:                            │
│ (Tương tự, cho target language)             │
└──────────────────────────────────────────────┘


🔧 CHIA SẺ TRỌNG SỐ (Weight Sharing):
3 nơi dùng CÙNG một embedding matrix:
1. Input embedding (Encoder)
2. Output embedding (Decoder)
3. Pre-softmax linear transformation

⚠️ Lưu ý: Trong embedding layers, nhân weights với √d_model = √512 ≈ 22.6


════════════════════════════════════════════════════════════════════════
OUTPUT & SOFTMAX:
════════════════════════════════════════════════════════════════════════

Decoder output → Linear transformation → Softmax → Probabilities

┌────────────────────────────────────────────┐
│ Decoder output: vector 512 chiều          │
│            ↓                               │
│ Linear: 512 → vocabulary_size (30000)     │
│            ↓                               │
│ Softmax: chuyển thành xác suất           │
│            ↓                               │
│ [P("Tôi")=0.01, P("Con")=0.02, ...]      │
│            ↓                               │
│ Chọn từ có xác suất cao nhất             │
└────────────────────────────────────────────┘

=====================================================================================

4.5. POSITIONAL ENCODING (MÃ HÓA VỊ TRÍ)

📚 TRẢ LỜI CÂU HỎI CỦA BẠN:

❓ POSITIONAL ENCODING LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

VẤN ĐỀ:
────────────────────────────────────────
Transformer KHÔNG có RNN, KHÔNG có CNN
→ Không biết thứ tự của các từ!

VÍ DỤ:
"Mèo đuổi chó" và "Chó đuổi mèo"
→ Nếu không biết thứ tự, model sẽ coi 2 câu này giống nhau!

GIẢI PHÁP: Positional Encoding
────────────────────────────────────────
Thêm thông tin về VỊ TRÍ vào mỗi từ

CÁC CÁCH TIẾP CẬN:

1️⃣ Cách đơn giản (nhưng không tốt):
   Position 1: [1, 0, 0, 0, ...]
   Position 2: [2, 0, 0, 0, ...]
   Position 3: [3, 0, 0, 0, ...]
   → Vấn đề: Số càng lớn, giá trị càng lớn (không công bằng)

2️⃣ Cách chuẩn hóa (vẫn không tốt):
   Position 1: [0.1, 0, 0, ...]
   Position 2: [0.2, 0, 0, ...]
   → Vấn đề: Phụ thuộc vào độ dài câu

3️⃣ Cách của Transformer (TỐT NHẤT): Sinusoidal
   → Dùng hàm sin và cos!


════════════════════════════════════════════════════════════════════════
CÔNG THỨC POSITIONAL ENCODING:
════════════════════════════════════════════════════════════════════════

PE(pos, 2i)   = sin(pos / 10000^(2i/d_model))
PE(pos, 2i+1) = cos(pos / 10000^(2i/d_model))

Trong đó:
• pos = vị trí của từ trong câu (0, 1, 2, 3, ...)
• i = index của dimension (0 đến 255, vì d_model/2 = 256)
• 2i = dimension chẵn → dùng sin
• 2i+1 = dimension lẻ → dùng cos


VÍ DỤ CỤ THỂ:
────────────────────────────────────────
Tính PE cho vị trí pos=1, các dimensions đầu tiên:

Dimension 0 (2i=0, i=0):
PE(1, 0) = sin(1 / 10000^(0/512)) = sin(1) ≈ 0.841

Dimension 1 (2i+1=1, i=0):
PE(1, 1) = cos(1 / 10000^(0/512)) = cos(1) ≈ 0.540

Dimension 2 (2i=2, i=1):
PE(1, 2) = sin(1 / 10000^(2/512)) = sin(1/1.045) ≈ 0.820

...và tiếp tục cho 512 dimensions


HÌNH ẢNH HÓA:
────────────────────────────────────────
            Dim 0  Dim 1  Dim 2  Dim 3  ...
Position 0: [0.00, 1.00, 0.00, 1.00, ...]
Position 1: [0.84, 0.54, 0.82, 0.57, ...]
Position 2: [0.91,-0.42, 0.93,-0.36, ...]
Position 3: [0.14,-0.99, 0.20,-0.98, ...]
...

→ Mỗi vị trí có một "dấu vân tay" độc nhất!


════════════════════════════════════════════════════════════════════════
TẠI SAO CHỌN SIN/COS?
════════════════════════════════════════════════════════════════════════

✅ ƯU ĐIỂM 1: Giới hạn trong khoảng [-1, 1]
   → Giá trị không bị bùng nổ

✅ ƯU ĐIỂM 2: Extrapolation (ngoại suy)
   → Có thể xử lý câu dài hơn câu trong training
   → VD: Training với câu tối đa 100 từ
        Testing với câu 150 từ → vẫn hoạt động!

✅ ƯU ĐIỂM 3: Mối quan hệ tương đối
   → PE(pos + k) có thể biểu diễn như linear function của PE(pos)
   → Model dễ học khoảng cách tương đối giữa các từ

VÍ DỤ:
   Khoảng cách từ vị trí 5 đến 10 = 5 bước
   Khoảng cách từ vị trí 20 đến 25 = 5 bước
   → Model nhận ra "pattern" này!


════════════════════════════════════════════════════════════════════════
CÁCH SỬ DỤNG TRONG TRANSFORMER:
════════════════════════════════════════════════════════════════════════

Embedding + Positional Encoding = Input Vector

VÍ DỤ với từ "love" ở vị trí 1:

Word Embedding("love")  = [0.2, 0.8, 0.1, 0.5, ...]  (512 dims)
Positional Encoding(1)  = [0.84, 0.54, 0.82, 0.57, ...] (512 dims)
                          ─────────────────────────────────
Final Input Vector      = [1.04, 1.34, 0.92, 1.07, ...] (CỘNG lại)

→ Thêm vào ở input của cả Encoder và Decoder


📊 THỰC NGHIỆM:
────────────────────────────────────────
Paper cũng thử learned positional embeddings
→ Kết quả gần như giống hệt sinusoidal
→ Nhưng chọn sinusoidal vì khả năng extrapolate


=====================================================================================

**PHẦN 5: TẠI SAO CHỌN SELF-ATTENTION?**

Phần này so sánh Self-Attention với Recurrent và Convolutional layers

════════════════════════════════════════════════════════════════════════
5.1. BA TIÊU CHÍ SO SÁNH:
════════════════════════════════════════════════════════════════════════

1️⃣ COMPLEXITY PER LAYER (Độ phức tạp tính toán)
   → Cần bao nhiêu phép tính?

2️⃣ PARALLELIZATION (Khả năng song song hóa)
   → Bao nhiêu phép tính phải làm tuần tự?

3️⃣ PATH LENGTH (Độ dài đường đi)
   → Thông tin cần đi qua bao nhiêu bước để kết nối 2 từ xa nhau?


════════════════════════════════════════════════════════════════════════
5.2. BẢNG SO SÁNH CHI TIẾT:
════════════════════════════════════════════════════════════════════════

Ký hiệu:
• n = độ dài sequence (số từ trong câu)
• d = dimension của representation (512)
• k = kernel width (cho convolutional)

┌──────────────────────┬─────────────┬────────────────┬──────────────┐
│ Layer Type           │ Complexity  │ Sequential Ops │ Max Path     │
│                      │ per Layer   │                │ Length       │
├──────────────────────┼─────────────┼────────────────┼──────────────┤
│ Self-Attention       │ O(n²·d)     │ O(1)          │ O(1)         │
├──────────────────────┼─────────────┼────────────────┼──────────────┤
│ Recurrent            │ O(n·d²)     │ O(n)          │ O(n)         │
├──────────────────────┼─────────────┼────────────────┼──────────────┤
│ Convolutional        │ O(k·n·d²)   │ O(1)          │ O(logₖ(n))   │
├──────────────────────┼─────────────┼────────────────┼──────────────┤
│ Self-Attention       │ O(r·n·d)    │ O(1)          │ O(n/r)       │
│ (restricted)         │             │                │              │
└──────────────────────┴─────────────┴────────────────┴──────────────┘


════════════════════════════════════════════════════════════════════════
5.3. PHÂN TÍCH CHI TIẾT TỪNG LOẠI:
════════════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1️⃣ SELF-ATTENTION LAYER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Complexity: O(n²·d)
─────────────────────────────────
Tính attention giữa mọi cặp từ: n × n = n² cặp
Mỗi phép tính có dimension d
→ Tổng: n² × d

VÍ DỤ: Câu 10 từ, dimension 512
n² × d = 10² × 512 = 51,200 phép tính

Sequential Operations: O(1)
─────────────────────────────────
TẤT CẢ các attention scores có thể tính SONG SONG!
→ Không cần chờ đợi gì cả
→ Chỉ 1 bước!

Max Path Length: O(1)
─────────────────────────────────
Bất kỳ 2 từ nào cũng kết nối TRỰC TIẾP qua attention
→ Thông tin đi ngay 1 bước!

VÍ DỤ: "The cat sitting on the mat is cute"
Từ "cat" ↔ "cute": 1 bước (trực tiếp)
Không quan tâm chúng cách nhau bao nhiêu từ!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2️⃣ RECURRENT LAYER (RNN/LSTM/GRU)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Complexity: O(n·d²)
─────────────────────────────────
Xử lý n từ
Mỗi từ: matrix multiplication d×d
→ Tổng: n × d²

VÍ DỤ: Câu 10 từ, dimension 512
n × d² = 10 × 512² = 2,621,440 phép tính
→ Nhiều hơn self-attention gần 50 lần!

Sequential Operations: O(n)
─────────────────────────────────
Phải xử lý TUẦN TỰ từng từ:
t₁ → t₂ → t₃ → ... → tₙ

⏱️ Không thể song song hóa!
Phải đợi xong t₁ mới tính t₂

Max Path Length: O(n)
─────────────────────────────────
Thông tin phải "truyền" qua tất cả các bước

VÍ DỤ: "The cat sitting on the mat is cute" (8 từ)
Từ "cat" (vị trí 2) → "cute" (vị trí 8)
→ Phải đi qua 6 bước!

Hậu quả: Với câu dài, thông tin đầu câu bị "mờ đi"


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3️⃣ CONVOLUTIONAL LAYER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Complexity: O(k·n·d²)
─────────────────────────────────
k = kernel width (VD: 3, 5, 7...)
n positions, mỗi position: k × d × d

VÍ DỤ: Câu 10 từ, kernel 3, dimension 512
k × n × d² = 3 × 10 × 512² = 7,864,320
→ Tốn kém nhất!

Sequential Operations: O(1)
─────────────────────────────────
Có thể song song hóa!
Tất cả convolutions tính cùng lúc

Max Path Length: O(logₖ(n))
─────────────────────────────────
Với dilated convolutions

VÍ DỤ: Kernel width k=3, câu 27 từ
log₃(27) = 3 layers cần để kết nối 2 đầu câu

Tốt hơn RNN nhưng vẫn tệ hơn Self-Attention!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
4️⃣ SELF-ATTENTION (RESTRICTED)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Complexity: O(r·n·d)
─────────────────────────────────
r = neighborhood size (số từ xung quanh được xem xét)
Chỉ nhìn r từ gần nhất, không nhìn toàn bộ

VÍ DỤ: r=5, chỉ nhìn 5 từ xung quanh
→ Giảm n² xuống r·n

Max Path Length: O(n/r)
─────────────────────────────────
Cần nhiều layers để thông tin đi xa


════════════════════════════════════════════════════════════════════════
5.4. KẾT LUẬN: TẠI SAO CHỌN SELF-ATTENTION?
════════════════════════════════════════════════════════════════════════

✅ ƯU ĐIỂM 1: NHANH với câu thông thường
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Khi n < d (thường là n < 512):
• n²·d < n·d²
• Self-Attention NHANH HƠN Recurrent!

VÍ DỤ:
Câu 50 từ (n=50), d=512
• Self-Attention: 50² × 512 = 1,280,000
• Recurrent: 50 × 512² = 13,107,200
→ Self-Attention nhanh hơn 10 lần!


✅ ƯU ĐIỂM 2: SONG SONG HÓA HOÀN TOÀN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Sequential Operations = O(1)
→ Tận dụng 100% sức mạnh GPU!
→ Training nhanh hơn RÁT NHIỀU


✅ ƯU ĐIỂM 3: HỌC LONG-RANGE DEPENDENCIES TỐT NHẤT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Max Path Length = O(1)
→ Mọi từ kết nối trực tiếp!
→ Không bị "quên" thông tin đầu câu

VÍ DỤ thực tế:
Câu: "The keys, which I had placed on the kitchen table yesterday 
      morning before leaving for work, are missing."
      
Từ "keys" và "are" cách nhau 13 từ
• RNN: thông tin đi qua 13 bước → bị mờ
• Self-Attention: kết nối trực tiếp → rõ ràng!


✅ ƯU ĐIỂM 4: DỄ HIỂU (INTERPRETABLE)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
Có thể visualize attention weights
→ Thấy được model "nhìn" vào đâu!

VÍ DỤ:
Khi dịch "it" trong "The animal didn't cross the street because it was tired"
→ Xem attention weights để biết "it" refer tới "animal" hay "street"


⚠️ NHƯỢC ĐIỂM: Với câu RẤT dài (n >> d)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
n² có thể rất lớn!

GIẢI PHÁP:
• Restricted Self-Attention (chỉ nhìn r từ gần nhất)
• Sparse Attention (các nghiên cứu sau này)
• Sliding Window Attention
• Longformer, BigBird, etc. (models sau này)

=====================================================================================

**PHẦN 6: QUÁ TRÌNH HUẤN LUYỆN (TRAINING)**

════════════════════════════════════════════════════════════════════════
6.1. DỮ LIỆU VÀ BATCHING
════════════════════════════════════════════════════════════════════════

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DỮ LIỆU TRAINING:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

📊 WMT 2014 English-German:
   • ~4.5 triệu cặp câu
   • Encode bằng Byte-Pair Encoding (BPE)
   • Vocabulary chung cho source và target: ~37,000 tokens
   
   VÍ DỤ BPE:
   "unhappiness" → ["un", "happiness"] → ["un", "happi", "ness"]
   → Xử lý tốt các từ hiếm!

📊 WMT 2014 English-French:
   • ~36 triệu câu (GẤP 8 LẦN!)
   • Word-piece vocabulary: 32,000 tokens


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
BATCHING (Gom nhóm):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Các câu được gom theo độ dài tương tự nhau
→ Tối ưu hóa memory và tốc độ

Mỗi batch chứa:
• ~25,000 source tokens
• ~25,000 target tokens

VÍ DỤ:
Batch 1: Các câu 10-15 từ
Batch 2: Các câu 15-20 từ
Batch 3: Các câu 20-30 từ


════════════════════════════════════════════════════════════════════════
6.2. PHẦN CỨNG VÀ THỜI GIAN
════════════════════════════════════════════════════════════════════════

🖥️ PHẦN CỨNG:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
8 × NVIDIA P100 GPUs (mỗi GPU ~16GB RAM)


⏱️ THỜI GIAN TRAINING:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Base Model:
• Tổng thời gian: ~12 giờ
• Tốc độ: ~0.4 giây/step
• Tổng số steps: ~100,000 steps

Big Model:
• Tổng thời gian: 3.5 ngày
• Tốc độ: ~1.0 giây/step  
• Tổng số steps: ~300,000 steps

💰 SO SÁNH CHI PHÍ:
→ RẺ hơn RẤT NHIỀU so với các models trước!
→ Kết quả lại TỐT HƠN!


════════════════════════════════════════════════════════════════════════
6.3. OPTIMIZER (BỘ TỐI ỰU HÓA)
════════════════════════════════════════════════════════════════════════

Sử dụng: ADAM OPTIMIZER

Tham số:
• β₁ = 0.9
• β₂ = 0.98
• ε = 10⁻⁹


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
LEARNING RATE SCHEDULE (Quan trọng!):
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Công thức:
lrate = d_model^(-0.5) · min(step^(-0.5), step · warmup_steps^(-1.5))

Hoặc viết dễ hiểu hơn:
lrate = (1/√512) × min(1/√step, step/warmup_steps^1.5)


CÁCH HOẠT ĐỘNG:
┌───────────────────────────────────────────────────┐
│ Phase 1: WARMUP (Steps 0 → 4000)                 │
│ Learning rate TĂNG dần từ 0 → max                │
│                                                   │
│     /                                             │
│    /                                              │
│   /                                               │
│  /                                                │
│ /___________________                              │
│ 0      4000                                       │
├───────────────────────────────────────────────────┤
│ Phase 2: DECAY (Steps 4000 → 300,000)            │
│ Learning rate GIẢM dần theo 1/√step              │
│                                                   │
│     \                                             │
│      \                                            │
│       \                                           │
│        \______                                    │
│         \_____\_____                              │
│         4000        300000                        │
└───────────────────────────────────────────────────┘

Warmup steps = 4000

TẠI SAO CẦN WARMUP?
• Đầu training, weights random → gradient không ổn định
• Learning rate nhỏ giúp model "khởi động" mượt mà
• Tránh model bị "sốc" và diverge (phân kỳ)


════════════════════════════════════════════════════════════════════════
6.4. REGULARIZATION (CHỐNG OVERFITTING)
════════════════════════════════════════════════════════════════════════

Áp dụng 3 kỹ thuật regularization:

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
1️⃣ RESIDUAL DROPOUT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Áp dụng dropout ở 2 vị trí:

Vị trí 1: Sau mỗi sub-layer
┌──────────────────────────────────────┐
│ Sub-layer output                     │
│         ↓                             │
│ Dropout (P_drop = 0.1)               │
│         ↓                             │
│ + Residual connection                │
│         ↓                             │
│ Layer Normalization                  │
└──────────────────────────────────────┘

Vị trí 2: Sau embedding + positional encoding
┌──────────────────────────────────────┐
│ Word Embedding + Positional Encoding │
│         ↓                             │
│ Dropout (P_drop = 0.1)               │
│         ↓                             │
│ Vào Encoder/Decoder                  │
└──────────────────────────────────────┘

P_drop = 0.1 → Randomly "tắt" 10% neurons


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
2️⃣ LABEL SMOOTHING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

ε_ls = 0.1

KHÔNG SỬ DỤNG LABEL SMOOTHING:
Target: "cat"
Label: [0, 0, 0, 1, 0, 0, ...] (1 cho "cat", 0 cho các từ khác)
       → Model quá "tự tin" 100%

SỬ DỤNG LABEL SMOOTHING:
Target: "cat"
Label: [0.01, 0.01, 0.01, 0.91, 0.01, 0.01, ...]
       → Phân một chút xác suất cho các từ khác
       → Model không quá tự tin

KẾT QUẢ:
• Perplexity tăng lên (model uncertain hơn)
• NHƯNG accuracy và BLEU score TỐT HƠN!
• Model generalize tốt hơn (không overfitting)

VÍ DỤ:
Khi dịch "good" → "tốt"
Không nói 100% là "tốt", mà 90% "tốt", 5% "hay", 5% "giỏi"...
→ Học được các từ đồng nghĩa!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
3️⃣ CHECKPOINT AVERAGING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Lưu model mỗi 10 phút
→ Lấy 8 checkpoints cuối cùng
→ Average weights của chúng
→ Model ổn định và tốt hơn!

=====================================================================================

**PHẦN 7: KẾT QUẢ (RESULTS)**

════════════════════════════════════════════════════════════════════════
7.1. KẾT QUẢ DỊCH MÁY (MACHINE TRANSLATION)
════════════════════════════════════════════════════════════════════════

📊 BLEU SCORE là gì?
─────────────────────────────────────────────────
BLEU (Bilingual Evaluation Understudy)
→ Chỉ số đo chất lượng dịch máy
→ So sánh bản dịch máy với bản dịch của người
→ Càng cao càng tốt (max = 100, thực tế ~40-50 là rất tốt)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🇩🇪 WMT 2014 ENGLISH → GERMAN
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Transformer (big): BLEU = 28.4
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ KỶ LỤC MỚI!
✨ Tốt hơn 2.0 BLEU so với record trước đó
✨ Chi phí training: CHỈ 1/4 so với models trước!

So sánh:
┌────────────────────────────┬──────────┬────────────┐
│ Model                      │ BLEU     │ Training   │
├────────────────────────────┼──────────┼────────────┤
│ Previous Best (ensemble)   │ 26.4     │ 14+ ngày   │
│ Transformer (base)         │ 27.3     │ 12 giờ     │
│ Transformer (big)          │ 28.4     │ 3.5 ngày   │
└────────────────────────────┴──────────┴────────────┘


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🇫🇷 WMT 2014 ENGLISH → FRENCH
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Transformer (big): BLEU = 41.0
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ KỶ LỤC MỚI cho single model!
✨ Chi phí < 1/4 so với model tốt nhất trước đó

Với Ensemble (kết hợp 8 models):
→ BLEU = 41.8 (CỰC KỲ ẤN TƯỢNG!)


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🏆 ENSEMBLE TECHNIQUE:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Lưu 8 checkpoints cuối (mỗi 10 phút)
2. Average predictions của tất cả 8 models
3. Sử dụng beam search với length penalty α=0.6

Kết quả:
• English-German: 28.4 → 30.2 BLEU
• English-French: 41.0 → 41.8 BLEU


════════════════════════════════════════════════════════════════════════
7.2. PHÂN TÍCH CÁC BIẾN THỂ MÔ HÌNH (MODEL VARIATIONS)
════════════════════════════════════════════════════════════════════════

ABLATION STUDY = Bỏ/thay đổi từng phần để xem phần nào quan trọng

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔬 THỰC NGHIỆM 1: Số lượng Attention Heads
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌────────────┬──────────┬─────────────────────┐
│ Số heads   │ BLEU     │ Nhận xét            │
├────────────┼──────────┼─────────────────────┤
│ 1          │ 24.9     │ ❌ Giảm 0.9 BLEU    │
│ 4          │ 25.5     │ ⚠️ Chưa tối ưu      │
│ 8 ⭐       │ 25.8     │ ✅ TỐT NHẤT         │
│ 16         │ 25.4     │ ⚠️ Giảm nhẹ         │
│ 32         │ 25.0     │ ❌ Quá nhiều heads  │
└────────────┴──────────┴─────────────────────┘

KẾT LUẬN: h = 8 là optimal cho d_model = 512


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔬 THỰC NGHIỆM 2: Attention Key Size (d_k)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌────────┬──────────┬─────────────────────────────┐
│ d_k    │ BLEU     │ Nhận xét                    │
├────────┼──────────┼─────────────────────────────┤
│ 32     │ 24.5     │ ❌ Quá nhỏ, hurt quality    │
│ 64 ⭐  │ 25.8     │ ✅ TỐT NHẤT                 │
│ 128    │ 25.7     │ ⚠️ Tương tự, tốn RAM hơn   │
└────────┴──────────┴─────────────────────────────┘

KẾT LUẬN: d_k quá nhỏ làm giảm khả năng biểu diễn


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔬 THỰC NGHIỆM 3: Kích thước Model
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌────────────┬───────┬──────────┬─────────────┐
│ d_model    │ d_ff  │ BLEU     │ Nhận xét    │
├────────────┼───────┼──────────┼─────────────┤
│ 256        │ 1024  │ 23.7     │ ❌ Quá nhỏ  │
│ 512 ⭐     │ 2048  │ 25.8     │ ✅ Base     │
│ 1024       │ 4096  │ 26.4     │ ✅ Big      │
└────────────┴───────┴──────────┴─────────────┘

KẾT LUẬN: 
• Model lớn hơn → kết quả tốt hơn
• Dropout rất quan trọng để tránh overfitting


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔬 THỰC NGHIỆM 4: Positional Encoding Type
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌──────────────────────┬──────────┬─────────────┐
│ Type                 │ BLEU     │ Nhận xét    │
├──────────────────────┼──────────┼─────────────┤
│ Sinusoidal ⭐        │ 25.8     │ ✅ Fixed    │
│ Learned              │ 25.7     │ ≈ Tương tự  │
└──────────────────────┴──────────┴─────────────┘

KẾT LUẬN: 
• Cả 2 cách đều tốt!
• Chọn Sinusoidal vì khả năng extrapolate


════════════════════════════════════════════════════════════════════════
7.3. ENGLISH CONSTITUENCY PARSING
════════════════════════════════════════════════════════════════════════

💡 ĐÂY LÀ THỰC NGHIỆM QUAN TRỌNG!
→ Chứng minh Transformer không chỉ tốt cho dịch máy
→ Mà có thể GENERALIZE sang các tasks khác!


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CONSTITUENCY PARSING LÀ GÌ?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Phân tích cấu trúc ngữ pháp của câu

VÍ DỤ:
Input: "The cat sleeps"

Output (parse tree):
                S
               / \
              NP  VP
             / \   |
           Det  N  V
            |   |  |
          The cat sleeps

→ Xác định chủ ngữ, động từ, cụm danh từ, cụm động từ...


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SETUP:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Dataset: Penn Treebank (Wall Street Journal)
• Training: ~40,000 câu

Model: Transformer (4 layers, d_model=1024)
• Không có task-specific tuning!
• Chỉ thay đổi kích thước model


━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
KẾT QUẢ:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌────────────────────────────────┬────────┐
│ Model                          │ F1     │
├────────────────────────────────┼────────┤
│ Previous Best Models           │ ~91.0  │
│ Transformer (WSJ only) ⭐      │ 91.3   │
│ Transformer (+ BerkleyParser)  │ 92.7   │
│ RNN Grammar (state-of-art)     │ 93.3   │
└────────────────────────────────┴────────┘

🎉 KẾT QUẢ ẤN TƯỢNG:
• Đứng thứ 2, chỉ sau RNN Grammar
• NHƯNG Transformer KHÔNG có task-specific tuning
• Chứng minh tính GENERAL của architecture!

=====================================================================================

**PHẦN 8: KẾT LUẬN VÀ Ý NGHĨA LỊCH SỬ**

════════════════════════════════════════════════════════════════════════
8.1. TÓM TẮT TRANSFORMER
════════════════════════════════════════════════════════════════════════

🏆 TRANSFORMER là mô hình sequence transduction ĐẦU TIÊN:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
• Dựa HOÀN TOÀN trên attention
• KHÔNG dùng recurrence (RNN/LSTM/GRU)
• KHÔNG dùng convolution (CNN)
• Thay thế bằng Multi-Headed Self-Attention


✅ ƯU ĐIỂM VƯỢT TRỘI:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. TỐC ĐỘ TRAINING:
   • Nhanh hơn NHIỀU LẦN so với RNN/CNN-based models
   • Song song hóa hoàn toàn trên GPU
   • Base model: 12 giờ vs nhiều ngày của models khác

2. CHẤT LƯỢNG:
   • English-German: 28.4 BLEU (improve 2.0 BLEU)
   • English-French: 41.8 BLEU (state-of-the-art)
   • Constituency Parsing: F1 = 92.7

3. HIỆU QUẢ:
   • Chi phí training < 1/4 so với models trước
   • Kết quả tốt hơn với chi phí thấp hơn

4. TÍNH TỔNG QUÁT:
   • Không chỉ tốt cho machine translation
   • Mà còn tốt cho parsing và các NLP tasks khác


════════════════════════════════════════════════════════════════════════
8.2. HƯỚNG PHÁT TRIỂN TƯƠNG LAI (Từ paper)
════════════════════════════════════════════════════════════════════════

📋 CÁC KẾ HOẠCH:

1️⃣ Áp dụng cho problems khác ngoài text:
   • Images (Computer Vision)
   • Audio (Speech Recognition)
   • Video (Video Understanding)

2️⃣ Nghiên cứu local, restricted attention:
   • Để xử lý hiệu quả large inputs/outputs
   • Giảm complexity từ O(n²) xuống O(n)

3️⃣ Making generation less sequential:
   • Tăng tốc độ inference
   • Parallel decoding


════════════════════════════════════════════════════════════════════════
8.3. TẦM QUAN TRỌNG LỊCH SỬ (Nhìn lại từ 2026)
════════════════════════════════════════════════════════════════════════

🌟 TRANSFORMER ĐÃ THAY ĐỔI HOÀN TOÀN NLP VÀ AI:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Paper này (2017) đã mở ra một kỷ nguyên mới!


📈 DÒNG THỜI GIAN:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

2017: Transformer ra đời
       ↓
2018: BERT (Google) - Bidirectional Encoder
      GPT-1 (OpenAI) - Decoder-only
       ↓
2019: GPT-2, RoBERTa, XLNet, ALBERT, T5
       ↓
2020: GPT-3 (175B parameters!)
       ↓
2021: Switch Transformer (1.6T parameters)
      DALL-E (text → image)
       ↓
2022: ChatGPT (GPT-3.5)
      Stable Diffusion
       ↓
2023: GPT-4, LLaMA, Claude, Gemini
      Multi-modal models
       ↓
2024-2026: Tiếp tục phát triển...


🏗️ CÁC MÔ HÌNH DỰA TRÊN TRANSFORMER:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

┌─────────────────────────────────────────────────────┐
│ ENCODER-ONLY (for understanding):                   │
│ • BERT, RoBERTa, ALBERT, ELECTRA                   │
│ • Dùng cho: classification, NER, QA...             │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ DECODER-ONLY (for generation):                      │
│ • GPT-1/2/3/4, LLaMA, Mistral, Gemini              │
│ • Dùng cho: text generation, chatbots...           │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ ENCODER-DECODER (for seq2seq):                      │
│ • T5, BART, mT5, mBART                             │
│ • Dùng cho: translation, summarization...          │
└─────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────┐
│ MULTI-MODAL:                                        │
│ • CLIP, DALL-E (text + image)                      │
│ • Whisper (audio)                                   │
│ • GPT-4V (text + image + audio)                    │
└─────────────────────────────────────────────────────┘


💡 NHỮNG ĐÓNG GÓP THEN CHỐT CỦA TRANSFORMER:
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1️⃣ CHỨNG MINH: Attention mechanism là ĐỦ!
   • Không cần RNN
   • Không cần CNN
   • Chỉ cần Attention!

2️⃣ SONG SONG HÓA HOÀN TOÀN:
   • Tận dụng được sức mạnh GPU
   • Training nhanh hơn nhiều lần
   • Mở đường cho Large Language Models

3️⃣ SCALABILITY:
   • Kiến trúc scale tốt
   • Từ 65M parameters (base) → hàng trăm tỷ parameters
   • Càng lớn càng tốt (với đủ dữ liệu)

4️⃣ TRANSFER LEARNING:
   • Pre-train 1 lần
   • Fine-tune cho nhiều tasks
   • Cơ sở cho BERT, GPT...

5️⃣ INTERPRETABILITY:
   • Có thể visualize attention
   • Hiểu được model "nghĩ" gì
   • Quan trọng cho research và debugging


════════════════════════════════════════════════════════════════════════
8.4. MÃ NGUỒN
════════════════════════════════════════════════════════════════════════

🔗 Code (TensorFlow):
https://github.com/tensorflow/tensor2tensor

🔗 PyTorch implementations (community):
• https://github.com/harvardnlp/annotated-transformer
• https://github.com/jadore801120/attention-is-all-you-need-pytorch


=====================================================================================

**PHẦN 9: CÁC KHÁI NIỆM QUAN TRỌNG - ÔN TẬP**

════════════════════════════════════════════════════════════════════════
THUẬT NGỮ CHÍNH:
════════════════════════════════════════════════════════════════════════

📚 EMBEDDING:
   Chuyển từ/token thành vector số
   VD: "mèo" → [0.2, 0.8, 0.1, ...]

📚 ATTENTION:
   Cơ chế giúp model "chú ý" vào đâu
   Tính toán mối quan hệ giữa các từ

📚 QUERY, KEY, VALUE:
   • Query: Điều đang tìm kiếm
   • Key: Tiêu đề/nhãn
   • Value: Nội dung thực sự

📚 SELF-ATTENTION:
   Attention trong cùng 1 sequence
   Các từ "nhìn thấy" nhau

📚 MULTI-HEAD ATTENTION:
   Nhiều attention song song
   Mỗi head học 1 kiểu mối quan hệ

📚 POSITIONAL ENCODING:
   Thêm thông tin vị trí vào embedding
   Dùng sin/cos functions

📚 RESIDUAL CONNECTION:
   x + F(x) thay vì chỉ F(x)
   Giúp training dễ hơn

📚 LAYER NORMALIZATION:
   Chuẩn hóa output của layer
   Ổn định training

📚 FEED-FORWARD NETWORK:
   MLP đơn giản: Linear → ReLU → Linear
   Áp dụng riêng cho từng vị trí

📚 ENCODER:
   Xử lý input sequence
   6 layers × (Self-Attention + FFN)

📚 DECODER:
   Tạo output sequence
   6 layers × (Masked Self-Att + Cross-Att + FFN)

📚 MASKING:
   Che thông tin tương lai
   Giữ tính auto-regressive

📚 BEAM SEARCH:
   Tìm kiếm output tốt nhất
   Giữ k candidates tốt nhất

📚 BLEU SCORE:
   Đo chất lượng dịch máy
   So sánh với bản dịch tham khảo


════════════════════════════════════════════════════════════════════════
CÔNG THỨC QUAN TRỌNG:
════════════════════════════════════════════════════════════════════════

1️⃣ Scaled Dot-Product Attention:
   Attention(Q,K,V) = softmax(QKᵀ/√dₖ)V

2️⃣ Multi-Head Attention:
   MultiHead(Q,K,V) = Concat(head₁,...,head₈)Wᴼ
   headᵢ = Attention(QWᵢQ, KWᵢK, VWᵢV)

3️⃣ Position-wise FFN:
   FFN(x) = max(0, xW₁+b₁)W₂+b₂

4️⃣ Positional Encoding:
   PE(pos,2i) = sin(pos/10000^(2i/d_model))
   PE(pos,2i+1) = cos(pos/10000^(2i/d_model))

5️⃣ Layer with Residual:
   Output = LayerNorm(x + Sublayer(x))


════════════════════════════════════════════════════════════════════════
THÔNG SỐ MÔ HÌNH:
════════════════════════════════════════════════════════════════════════

BASE MODEL:
• Layers: N = 6 (encoder + decoder)
• d_model = 512
• d_ff = 2048
• h = 8 heads
• d_k = d_v = 64
• P_drop = 0.1

BIG MODEL:
• Layers: N = 6
• d_model = 1024
• d_ff = 4096
• h = 16 heads
• d_k = d_v = 64
• P_drop = 0.3